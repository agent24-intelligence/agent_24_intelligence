# AGENTS.md

이 저장소는 AGENT:24 해커톤(24시간, Liner API Agent 트랙) 제출물입니다.
아래 지침은 저장소 전체에 적용됩니다. 매번 반복해서 지시하지 마세요.

## 프로젝트 개요

- **컨셉**: 적용 갭 파인더 — 연구/기술 주제를 입력하면 학계-산업 간 적용 격차를 찾아 근거와 함께 보여주는 에이전트
- **필수 스택**: Liner API (Search, Deep Research, Visualization) + OpenAI Agents SDK
- **파이프라인**: 학계 스캔(Search) → 산업 스캔(Search) → 대조·격차 판정 → 근거 부족 시 조건부 Deep Research 승격 → Visualization
- **오케스트레이션**: 큰 모델 = 플래닝/갭 판단, 작은 모델 = 쿼리 생성·요약·포맷팅 (작업별로 분리해서 호출)

## 빌드 원칙 (24시간 한정)

이건 프로덕션이 아니다. 데모가 라이브로 한 번 정상 작동하는 것이 유일한 목표다.

- **테스트/CI 작성 금지**: 유닛 테스트, 통합 테스트, 커버리지, CI 파이프라인 만들지 마라.
- **과도한 방어 코드 금지**: 에러 핸들링은 최소한으로. 다만 실패 자체는 숨기지 말 것 (아래 코드 스타일 참고).
- **불필요한 리팩토링 금지**: 지금 도는 코드는 건드리지 말고 다음 기능으로 넘어가라.
- **선제적 구현 금지**: "나중에 필요할 것 같아서" 미리 만들지 마라. 지금 데모에 필요한 것만 만든다.
- **새 스택 추가 금지**: 이미 쓰고 있는 라이브러리/프레임워크로 해결되면 그걸 써라.
- **자정(00:00) 이후**: 새 기능 추가보다 안정화·버그수정 우선. 이미 도는 파이프라인은 되도록 건드리지 않는다.
- **커밋**: 8/1 14:00 이후 것만 인정된다. 커밋 메시지는 짧고 명확하게, 무엇을 했는지 알 수 있게 남긴다.

## 파이프라인 구현 규칙

- 각 Liner API 호출 전후로 **왜 이 단계로 넘어갔는지**를 짧은 로그/텍스트로 남겨라.
  예: `"1차 스캔 근거 부족 → Deep Research로 승격"`
  → Raw API Stream 화면에 tool_call/tool_result와 함께 노출되어야 한다.
- Deep Research API는 비용이 크므로 **무조건 호출하지 말 것**. 1차 Search 결과의 근거 충분성을 판단한 뒤에만 조건부로 승격한다. 판단 임계값은 하드코딩하지 말고 프롬프트/설정값으로 분리해서 쉽게 조정 가능하게 한다.
- 모델 분리: 갭이 유의미한지 판단하는 최종 의사결정은 큰 모델에게, 검색 쿼리 생성·결과 요약·포맷팅 같은 반복 작업은 작은/저렴한 모델에게 맡긴다.

### 엣지케이스 — 반드시 정직하게 처리, 억지로 결과 만들지 말 것

| 입력 유형 | 처리 방식 |
|---|---|
| 너무 광범위 (예: "AI") | 스스로 범위를 좁혀서 대표 사례로 진행 |
| 너무 니치 | "충분한 근거 확보 못함"을 그대로 보고 |
| 존재하지 않는/가짜 분야 | "확인 안 됨"을 그대로 보고 |
| 이미 완전히 산업화된 분야 | "갭 없음, 과잉적용 상태"로 판정 뒤집기 |

## 텍스트/문구 톤 가이드

발표 슬라이드, UI 문구, 데모 화면 텍스트, 결과 리포트 등 모든 사용자 대상 텍스트에 적용한다.
규칙마다 Before(❌) / After(✅)를 참고해서 그 패턴 그대로 따라 쓴다.

**1. 제목 — 타이틀 케이스 금지**
- ❌ "AI 기반 지식 격차 탐지 솔루션" / "Discover The Application Gap Instantly"
- ✅ "학계-산업 갭 파인더" / "Application Gap Finder"
- 이유: 수식어(AI 기반, 솔루션) 제거하고 뭘 하는지만 남긴다. 영문 제목도 단어마다 대문자로 시작하지 않는다.

**2. 설명 문구 — 과장 수식어·느낌표 금지**
- ❌ "혁신적인 AI 에이전트가 순식간에 인사이트를 발견합니다!" / "차세대 리서치 경험을 지금 만나보세요"
- ✅ "논문과 산업 사례를 대조해서 아직 적용 안 된 지점을 찾습니다." / "주제 하나를 입력하면 학계-산업 간 격차를 근거와 함께 정리합니다"
- 이유: "혁신적인", "차세대", "패러다임을 바꾸는" 같은 과장 수식어와 느낌표를 빼고, 실제 동작을 그대로 서술한다.

**3. 결과 리포트 — 근거·숫자 중심, 상투적 마무리 금지**
- ❌ "이 결과를 한눈에 확인하세요! 🎉"
- ✅ "관련 논문 12건, 산업 사례 3건 검토 결과 — 근거 신뢰도 중간"
- 이유: 이모지·감탄사 없이, 검토한 근거 수와 신뢰도를 담백하게 제시한다.

애매한 경우엔 "숫자·동작 위주로 짧게" 쪽을 항상 우선한다.

## 코드 스타일

- 언어/프레임워크는 팀이 정한 스택을 그대로 따른다 (별도 합의 없이 변경하지 말 것).
- 환경변수/API 키는 `.env`에 두고 절대 커밋하지 않는다.
- 에러가 나면 조용히 삼키지 말고, Raw API Stream 화면에 실패 사실 자체를 노출한다. 실패도 정직하게 보여주는 것이 이 컨셉의 핵심이다.

## 발표 관련 하드 제약 (규칙상 위반 불가)

- 슬라이드 최대 5장 (6장 이상 제작 금지)
- 사전 녹화 영상으로 라이브 데모 대체 금지 (결선 발표 기준). 단 제출용 2분 데모 영상은 예외.