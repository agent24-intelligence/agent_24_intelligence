# AGENTS.md

이 저장소는 AGENT:24 해커톤(24시간, Liner API Agent 트랙) 제출물입니다.
아래 지침은 이 저장소 전체에 적용됩니다. 매번 반복해서 지시하지 마세요.

## 프로젝트 개요

- **컨셉**: 적용 갭 파인더 — 연구/기술 주제를 입력하면 학계-산업 간 적용 격차를 찾아 근거와 함께 보여주는 에이전트
- **필수 스택**: Liner API (Search, Deep Research, Visualization) + OpenAI Agents SDK
- **파이프라인**: 학계 스캔(Search) → 산업 스캔(Search) → 대조·격차 판정 → 근거 부족 시 조건부 Deep Research 승격 → Visualization
- **오케스트레이션**: 큰 모델 = 플래닝/갭 판단, 작은 모델 = 쿼리 생성·요약·포맷팅 (비용·속도 위해 작업별로 분리해서 호출)

## 빌드 원칙 (24시간 한정)

- **이건 프로덕션이 아니다.** 유닛 테스트, 커버리지, CI 파이프라인, 과도한 에러 핸들링 만들지 마라. 데모가 라이브로 한 번 정상 작동하는 것이 유일한 목표다.
- 정말 필요한 게 아니면 리팩토링하지 마라. 지금 도는 코드는 건드리지 말고 다음 기능으로 넘어가라.
- 새 라이브러리/프레임워크를 무턱대고 추가하지 마라. 이미 쓰고 있는 스택으로 해결되면 그걸 써라.

## 파이프라인 구현 규칙

- 각 Liner API 호출 전후로 **왜 이 단계로 넘어갔는지**를 짧은 로그/텍스트로 남겨라 (예: `"1차 스캔 근거 부족 → Deep Research로 승격"`). 이게 Raw API Stream 화면에 tool_call/tool_result와 함께 노출되어야 한다.
- Deep Research API는 비용이 크므로 **무조건 호출하지 말고**, 1차 Search 결과의 근거 충분성을 판단한 뒤에만 조건부로 승격하라. 이 판단 기준(임계값)은 하드코딩하지 말고 프롬프트/설정값으로 분리해서 쉽게 조정 가능하게 하라.
- 아래 4가지 입력 케이스는 반드시 정직하게 처리하고, 억지로 결과를 만들어내지 마라:
  1. 너무 광범위한 입력 → 스스로 범위를 좁혀서 대표 사례로 진행
  2. 너무 니치한 입력 → "충분한 근거 확보 못함"을 그대로 보고
  3. 존재하지 않는/가짜 분야 → "확인 안 됨"을 그대로 보고
  4. 이미 완전히 산업화된 분야 → "갭 없음, 과잉적용 상태"로 판정 뒤집기
- 모델 분리: 갭이 유의미한지 판단하는 최종 의사결정은 큰 모델에게, 검색 쿼리 생성·결과 요약·포맷팅 같은 반복 작업은 작은/저렴한 모델에게 맡겨라.

## 텍스트/문구 톤 가이드 (UI, 슬라이드, 데모 화면 전부 적용)

생성되는 모든 사용자 대상 텍스트(발표 슬라이드, UI 문구, 결과 리포트)는 아래 규칙을 따른다:

- 제목에 **타이틀 케이스(Title Case) 금지**. 자연스러운 문장형 또는 한국어 어순으로 쓴다.
- 번역투/마케팅 말투 금지. "혁신적인", "차세대", "패러다임을 바꾸는" 같은 과장된 수식어 쓰지 마라.
- 엔지니어 매뉴얼 톤으로 쓴다: 담백하고 구체적으로, 근거와 숫자 중심으로 서술한다.
- 이모지, 과도한 느낌표, AI가 생성한 티가 나는 상투적 문구("한눈에 확인하세요!" 류) 금지.

## 코드 스타일

- 언어/프레임워크는 팀이 정한 스택을 그대로 따른다 (별도 합의 없이 변경하지 말 것).
- 환경변수/API 키는 `.env`에 두고 절대 커밋하지 않는다.
- 에러가 나면 조용히 삼키지 말고, Raw API Stream 화면에 실패 사실 자체를 노출한다 (실패도 정직하게 보여주는 것이 이 컨셉의 핵심이다).

## 하지 말아야 할 것

- 유닛 테스트/통합 테스트 작성
- 사용되지 않는 기능의 사전 구현 ("나중에 필요할 것 같아서")
- 슬라이드 6장 이상 제작 (규칙상 최대 5장)
- 사전 녹화 영상으로 라이브 데모 대체 (결선 발표 기준 금지, 단 제출용 2분 데모 영상은 예외)